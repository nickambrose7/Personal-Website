# Repository Instructions

- Any UI or styling change must be reviewed in both light mode and dark mode before it is considered complete.
- Reuse the theme variables, classes, and primitives already provided by the app and its UI libraries for light/dark behavior instead of introducing isolated color systems.
